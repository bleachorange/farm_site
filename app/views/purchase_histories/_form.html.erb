<%= form_for(@purchase_history) do |f| %>
  <% if @purchase_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase_history.errors.count, "error") %> prohibited this purchase_history from being saved:</h2>

      <ul>
      <% @purchase_history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :profile_id %><br>
    <%= f.text_field :profile_id %>
  </div>
  <div class="field">
    <%= f.label :purchase_id %><br>
    <%= f.text_field :purchase_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
